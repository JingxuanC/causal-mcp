numpy>=1.26,<2
scipy>=1.10
pandas>=2.0
statsmodels>=0.14
scikit-learn>=1.3
# ── 可选：causal_impact 的真 BSTS 路径（Google CausalImpact 移植；
#    未装时自动降级为 OLS 对冲回归，method="ols_hedge"）──
# pycausalimpact>=0.1
# ── 可选：pcmci_discover 的真 PCMCI 路径（Runge et al. 2019；
#    未装时自动降级为纯 numpy lagged 条件偏相关，method="granger_fallback"）──
# tigramite>=5.2
# ── 可选：dml_cate 的 LinearDML 路径（Chernozhukov et al. 2018；
#    未装时自动降级为 sklearn T-learner，method="t_learner"）──
# econml>=0.15
# ── 可选：完整 DoWhy 反事实估计（本项目的 refute 工具是 DoWhy 方法论的
#    纯 numpy 自包含实现，不依赖 dowhy；需要完整 DoWhy 管线时自行安装）──
# dowhy>=0.11
# networkx>=3.0
