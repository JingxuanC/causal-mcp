numpy>=1.26,<2
scipy>=1.10
pandas>=2.0
# ── 可选：learn_graph 的 method="notears" 路径（未装时自动降级为相关性边）──
# scikit-learn>=1.3
# ── 可选：完整 DoWhy 反事实估计（本项目的 refute 工具是 DoWhy 方法论的
#    纯 numpy 自包含实现，不依赖 dowhy；需要完整 DoWhy 管线时自行安装）──
# dowhy>=0.11
# networkx>=3.0
# ── 可选：statsmodels 风格 OLS 诊断（当前事件研究用 numpy lstsq + scipy
#    t 检验实现，未直接依赖 statsmodels）──
# statsmodels>=0.14
